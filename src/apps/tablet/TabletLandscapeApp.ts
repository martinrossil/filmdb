import { Container } from 'fuix';

export default class TabletLandscapeApp extends Container {
    public constructor() {
        super();
    }
}
customElements.define('tablet-landscape-app', TabletLandscapeApp);
